{
  "name": "spark-platform",
  "version": "0.0.1",
  "description": "Node.JS interface for the SparkPlatform API",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha --require source-map-support --require babel-core/register spec/spec_setup.js --require spec/spec_helper.js --recursive spec/**/*.spec.js --timeout 10000",
    "test:single": "mocha --require source-map-support --require babel-core/register  spec/spec_setup.js --require spec/spec_helper.js --timeout 10000",
    "build": "babel -d lib/ src/",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kolyaventuri/node-spark.git"
  },
  "keywords": [
    "spark",
    "platform",
    "spark",
    "real",
    "estate",
    "mls"
  ],
  "author": "Kolya Venturi",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/kolyaventuri/node-spark/issues"
  },
  "homepage": "https://github.com/kolyaventuri/node-spark#readme",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-stage-0": "^6.24.1",
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "pryjs": "^1.0.3",
    "source-map-support": "^0.5.6"
  },
  "dependencies": {
    "request": "^2.87.0",
    "request-promise": "^4.2.2"
  }
}
